import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root')); //unica vez que vamos utilizar getelementbyid

root.render(

<App /> //este es un componente, el componente padre
);

